# Logs
*.log
logs/

# Environment variables and local overrides
.env
.env.local
.env.*.local

# Chrome extension signing keys
*.pem
*.crx

# Packaged outputs (zip files)
*.zip

# Historical store-assets (release zips + marketing copy) live in
# ~/work/after-bedtime/archives/ptac-store-assets/ — never in this repo
store-assets/

# Build artifacts (if ever introduced)
dist/
build/
out/

# Dependency directories (if ever introduced)
node_modules/

# Temporary files
tmp/
temp/
*.tmp
*.bak
